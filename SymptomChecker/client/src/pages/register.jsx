//import { Link } from "react-router-dom"

/*
import '../styles/login.css'
import {useForm} from 'react-hook-form'
import { instance as axios } from '../api/axios';

const Register = () => {
    const {register, handleSubmit} = useForm();

    const onSubmit = handleSubmit(async values =>{
        //console.log(values);
        axios.post("/register",values).then(
            (res) => {
                console.log(res);
            }
        ).catch((err) => console.error(err));
    })

  return (
    <form onSubmit={onSubmit}>
        <h1 class="title">Register</h1>
        <label>
            <i class="fa-solid fa-user"></i>
            <input placeholder="email" type="text" id="email" {...register("email")}/>
        </label>
        <label>
            <i class="fa-solid fa-user"></i>
            <input placeholder="username" type="text" id="username" {...register("name")}/>
        </label>
        <label>
            <i class="fa-solid fa-lock"></i>
            <input placeholder="password" type="password" id="password" {...register("password")}/>
        </label>
        

        <button id="button">Register</button>

    </form>
  )
}

export default Register
*/